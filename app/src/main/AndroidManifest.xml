<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solomode.project.uminaja">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TipsActivity.Intro"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TipsActivity.TipsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity.SplashActivity" />
        <activity android:name=".MainActivity.MainActivity" />
        <activity android:name=".MatkulActivity.MatkulActivity" />
        <activity android:name=".SoalActivity.SoalActivity" />
        <activity android:name=".InsertReportActivity.InsertReportActivity" />
        <activity android:name=".AboutActivity.AboutActivity" />
    </application>

</manifest>